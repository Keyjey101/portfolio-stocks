#!/usr/bin/env node
/**
 * Портфельный дашборд: сигналы входа + уровни по позициям
 * Запуск:  node dashboard.js     →  http://localhost:3000
 * Требует Node 18+ (встроенный fetch). Зависимостей нет.
 * Данные: Yahoo Finance (бесплатно, без ключа).
 *
 * Структура:
 *   dashboard.js      — точка входа
 *   src/tradernet.js  — позиции из Tradernet API (ключи в .env)
 *   src/portfolio.js  — мета позиций (теги, уровни, заметки)
 *   src/yahoo.js      — загрузка котировок и утилиты рядов
 *   src/signals.js    — рыночные сигналы, вердикт, кэш данных
 *   src/server.js     — http-роутер: /, /api/data, статика public/
 *   public/           — index.html, style.css, app.js (клиент)
 *
 * Позиции (тикер/кол-во/цена входа) приходят из Tradernet API:
 * покупка/продажа у брокера — терминал сам видит изменения
 * (кэш позиций 60 сек, клиент обновляется раз в 90 сек).
 *
 * Дизайн v4 «серебряные приборы»: светлый серебристо-серый холст,
 * белые приборы в серых лотках (double-bezel), бронзовый акцент,
 * зарево фона перекрашивается вердиктом (g/y/r), зерно поверх.
 * Manrope + Unbounded + IBM Plex Mono.
 */

require('./src/server').start();
